# iTechTurboAssignment

The app is the fullscreen map & webview as a bottom sheetwith smooth scrolling behaviour & animations.

Note: the app requests user's location every 5 sec & moves/zooms camera to new coordinates (if necessary).
